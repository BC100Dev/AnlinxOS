#ifndef ANLINOS_UTILS_HPP
#define ANLINOS_UTILS_HPP

#include <vector>
#include <string>

std::vector<std::string> translateCmdLine(const std::string &cmdLine);

std::string TrimString(const std::string& str);

#endif //ANLINOS_UTILS_HPP
